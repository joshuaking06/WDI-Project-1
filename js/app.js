$(() => {


  const $shipGrid = $('.ship-grid')
  const $fireGrid = $('.fire-grid')
  const width = 10

  for(let i = 0; i<width*width; i++) {
    $shipGrid.append($('<div />'))
    $fireGrid.append($('<div />'))
  }

  








})
